Hello Admin, 

someone added a new comment!

Blogname: 			{settings.blogName}
Blogpost: 			{post.title}

<f:uri.action
	pluginName="Blogsystem"
	controller="Post"
	action="show"
	pageUid="{settings.blogsystem.pid}"
	absolute="true"
	arguments="{post: post, day: post.publishDay, month: post.publishMonth, year: post.publishYear}"
	section="comment_{comment.uid}" />

Comment title: 		{comment.title}
Comment author: 	{comment.author}
Comment date: 		<f:format.date format="d.m.Y - H:i:s">{comment.date}</f:format.date>
Comment website: 	{comment.website}
Comment approved: 	<f:if condition="{comment.approved}"><f:then>yes</f:then><f:else>no</f:else></f:if>
Comment is spam: 	<f:if condition="{comment.spam}"><f:then>yes</f:then><f:else>no</f:else></f:if>
Comment text: 
{comment.text}


You could manage comments in the TYPO3 backend of your installation:
{domain}typo3

Regards
{settings.blogName} 
 